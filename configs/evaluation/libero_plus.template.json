{
  "schema_version": "bas_vla_evaluation_v1",
  "status": "requires_artifacts",
  "protocol": "libero_plus",
  "benchmark_revision": "REQUIRED immutable benchmark revision",
  "selection_id": "REQUIRED exact evaluated selection identifier",
  "environment_factory": "bas_vla.evaluation.openpi:create_env",
  "carrier": {
    "name": "REQUIRED carrier name",
    "checkpoint_id": "REQUIRED checkpoint artifact identifier",
    "config_name": "REQUIRED carrier config",
    "normalization_key": "REQUIRED checkpoint normalization key",
    "checkpoint_files": {}
  },
  "cases": [],
  "required_case_fields": [
    "case_id",
    "category",
    "instruction",
    "bddl_file",
    "bddl_file_sha256",
    "init_states_file",
    "init_states_file_sha256",
    "normalization_key",
    "trials",
    "goal"
  ],
  "trial_schema": {
    "seed": "integer",
    "episode_idx": "integer"
  },
  "asset_paths": "Relative to --asset-root; immutable source files with SHA-256 digests.",
  "selection_note": "Configure the exact case selection and trial identities for the evaluation.",
  "paper_budget": {
    "categories": {
      "light": 10,
      "background": 10,
      "noise": 10,
      "language": 10
    },
    "rollouts_per_case_per_method": 120
  },
  "environment_note": "Use the matching LIBERO-Plus fork and perturbation assets. For runtime perturbations beyond BDDL, provide an environment_factory wrapper with explicit env_kwargs."
}
