# Release contents

The repository includes method components, carrier runners, evaluation tools,
protocol configurations, statistics utilities, and the paper/project page.
Third-party carrier code, model weights, benchmark assets, and hardware drivers
are installed separately under their own licenses.

The [reproduction guide](../docs/reproduction.md) lists the available workflows
and assets required for each evaluation. The synthetic records example illustrates the training data format.
